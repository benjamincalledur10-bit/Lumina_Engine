plugins {
    id 'net.fabricmc.fabric-loom' version "${loom_version}"
}

version = project.mod_version
group = project.maven_group
base.archivesName = 'lumina-engine-fabric-26.2'

loom {
    splitEnvironmentSourceSets()

    mods {
        lumina_engine {
            sourceSet sourceSets.main
            sourceSet sourceSets.client
        }
    }
}

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_26_2}"
    implementation "net.fabricmc:fabric-loader:${loader_version}"
    implementation "net.fabricmc.fabric-api:fabric-api:${fabric_api_26_2}"
    implementation "dev.isxander:yet-another-config-lib:${yacl_26_2}"
    clientImplementation project(':common')
}

processResources {
    inputs.property 'version', project.version
    filesMatching('fabric.mod.json') {
        expand 'version': project.version
    }
}

tasks.withType(JavaCompile).configureEach {
    options.release = 25
    options.encoding = 'UTF-8'
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(25)
    withSourcesJar()
}

jar {
    from project(':common').sourceSets.main.output
    from(rootProject.file('LICENSE')) {
        rename { 'LICENSE_Lumina_Engine' }
    }
}

sourcesJar {
    from project(':common').sourceSets.main.allSource
}
